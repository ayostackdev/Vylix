# Core
fastapi>=0.115.0
alembic>=1.13.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0
asyncpg>=0.30.0
psycopg[binary]>=3.2.0

# Redis / Celery
celery>=5.4.0
redis>=5.0.0

# HTTP client
httpx>=0.27.0

# Cloudflare R2 (S3-compatible object storage)
boto3>=1.35.0

# JWT
PyJWT>=2.8.0

# Image processing (lightweight)
Pillow>=10.4.0
# Handout OCR on the API process (/documents/ocr). The tesseract binary itself
# is apt-installed in the base image; the opencv shadow-destroyer handles
# unevenly-lit phone photos before pytesseract runs.
opencv-python-headless>=4.9.0.80
pillow-heif>=0.15.0
pytesseract>=0.3.13

# PDF processing
PyMuPDF>=1.24.0

# AI / ML (lightweight - no torch/transformers/easyocr/docling)
numpy>=2.1.0
scikit-learn>=1.5.0
google-genai>=1.0.0

# Rate limiting
slowapi>=0.1.9

# Error tracking
sentry-sdk[fastapi]>=2.0.0

# Google Drive
google-auth-oauthlib>=1.2.0
psycopg-pool>=3.2.0
