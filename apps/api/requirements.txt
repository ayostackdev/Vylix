# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0
asyncpg>=0.30.0
psycopg[binary]>=3.2.0

# Redis / Celery
celery>=5.4.0
redis>=5.0.0

# HTTP client
httpx>=0.27.0

# Cloudflare R2 (S3-compatible object storage)
boto3>=1.35.0

# JWT
PyJWT>=2.8.0

# Image processing
Pillow>=10.4.0
# Handout "Shadow Destroyer" (uneven-lighting binarization before OCR)
opencv-python-headless>=4.9.0.80
# iPhone snaps upload as HEIC — needed to decode them before OCR
pillow-heif>=0.15.0

# PDF processing
PyMuPDF>=1.24.0

# AI / ML
numpy>=2.1.0
scikit-learn>=1.5.0
google-genai>=1.0.0

# Self-hosted embeddings (dense + sparse); pulls torch (already present via docling)
FlagEmbedding>=1.2.10

# OCR (image uploads): tesseract binary is apt-installed in the base image
pytesseract>=0.3.13

# Document parsing
docling>=2.0.0

# Rate limiting
slowapi>=0.1.9

# Error tracking
sentry-sdk[fastapi]>=2.0.0

# Google Drive
google-auth-oauthlib>=1.2.0
psycopg-pool>=3.2.0
