fastapi[all]>=0.115.0
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
# ChromaDB vector fallback: local/dev-only stand-in when pgvector is not wired up
# (production always uses pgvector, so this stays out of requirements.txt)
chromadb>=0.5.0
